*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc
*.pem
node_identity.json

# machine-local secrets pointer (symlink to ~/dev/.env) — never track
.env

# machine-local host map (real IPs + Mac SSH user) — see LOCAL_HOSTS.md for the
# placeholder → actual mapping; never committed.
LOCAL_HOSTS.md

# dev-kid local config (per-machine scaffold; contains real LAN IPs in
# sentinel.tier1.ollama_url). Re-generated by `dev-kid init --lightweight`.
dev-kid.yml
